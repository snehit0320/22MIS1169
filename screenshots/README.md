# Stage 1 screenshots

Add a terminal screenshot here after running:

```bash
cd notification_app_be
node priorityNotifications.js <your-token>
```

The output should show **"Stage 1 — Top 10 Priority Notifications"** with 10 ranked lines.
